[project]
name = "voice-browser"
version = "0.1.0"
description = "Local web UI to listen to all Pocket TTS catalog voices"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "pocket-tts>=2.1.0",
    "scipy>=1.17.1",
    "uvicorn>=0.52.4",
]
